name: kite-token-to-lightsail

on:
  schedule:
    # 08:00 IST = 02:30 UTC
    - cron: "30 2 * * 1-5"
  workflow_dispatch:

jobs:
  token:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable
          install-chromedriver: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install kiteconnect selenium pyotp

      - name: Write SSH key + known_hosts
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.LIGHTSAIL_SSH_KEY }}" > ~/.ssh/lightsail_key
          chmod 600 ~/.ssh/lightsail_key
          ssh-keyscan -H "${{ secrets.LIGHTSAIL_HOST }}" >> ~/.ssh/known_hosts

      - name: Generate token + upload to Lightsail
        env:
          KITE_API_KEY: ${{ secrets.KITE_API_KEY }}
          KITE_API_SECRET: ${{ secrets.KITE_API_SECRET }}
          KITE_USER_ID: ${{ secrets.KITE_USER_ID }}
          KITE_PASSWORD: ${{ secrets.KITE_PASSWORD }}
          KITE_PIN: ${{ secrets.KITE_PIN }}
          # Or use TOTP instead of PIN:
          # KITE_TOTP_SECRET: ${{ secrets.KITE_TOTP_SECRET }}
          HEADLESS: "1"
          KITE_TOKEN_PATH: ./secrets/kite_access_token.json
        run: |
          python scripts/get_kite_access_token_selenium.py
          scp -i ~/.ssh/lightsail_key ./secrets/kite_access_token.json \
            "${{ secrets.LIGHTSAIL_USER }}@${{ secrets.LIGHTSAIL_HOST }}:/opt/kite/secrets/kite_access_token.json"
